# Copy to .env.local. Never commit the filled file.
EKT_API_USER=
EKT_API_PASSWORD=
# Required to enable EKT AI. Full HTTPS Chat Completions endpoint.
# Model must support function calling and max_completion_tokens.
# Keep the key on the server; never use NEXT_PUBLIC_ variables.
LLM_URL=
LLM_API_KEY=
LLM_MODEL=
