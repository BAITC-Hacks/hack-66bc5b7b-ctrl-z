"use client";
import {useEffect} from 'react';
export default function WidgetDemo(){useEffect(()=>{const s=document.createElement('script');s.src='/widget.js';s.async=true;document.body.appendChild(s);return()=>{s.remove();(window as any).EKTWidget?.destroy()}},[]);return <main className="embed-demo"><a href="/">← Вернуться к консультанту</a><span className="eyebrow">ДЕМОНСТРАЦИЯ ВСТРАИВАНИЯ</span><h1>Консультант рядом с каталогом</h1><p>Кнопка в правом нижнем углу открывает рабочий чат поверх страницы. Диалог и демонстрационная корзина используют ту же сессию.</p><div className="embed-explanation"><h2>Как подключить на сайт</h2><p>Разместите приложение на домене магазина через обратный прокси и добавьте скрипт перед закрывающим тегом body:</p><pre>{'<script src="/widget.js" defer></script>'}</pre><p>При подключении с другого домена кнопка открывает консультанта в отдельной вкладке. Это сохраняет работу сессии без ослабления защиты cookies.</p><p>Встраивание на настоящий ekt.kz требует доступа владельца сайта. Прямое управление его корзиной требует отдельного API; демонстрационная корзина не создаёт заказов.</p></div></main>}
