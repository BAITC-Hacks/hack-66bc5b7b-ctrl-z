"use client";
import {useLocale,LanguagePicker} from '../locale';
import {useEffect} from 'react';
export default function WidgetDemo(){const {t,locale}=useLocale();useEffect(()=>{const s=document.createElement('script');s.src='/widget.js';s.async=true;s.dataset.locale=locale;document.body.appendChild(s);return()=>{s.remove();(window as any).EKTWidget?.destroy()}},[locale]);return <main className="embed-demo"><LanguagePicker/><a href="/">{t("← Вернуться к консультанту")}</a><span className="eyebrow">{t("ДЕМОНСТРАЦИЯ ВСТРАИВАНИЯ")}</span><h1>{t("Консультант рядом с каталогом")}</h1><p>{t("Кнопка в правом нижнем углу открывает рабочий чат поверх страницы. Диалог и демонстрационная корзина используют ту же сессию.")}</p><div className="embed-explanation"><h2>{t("Как подключить на сайт")}</h2><p>{t("Разместите приложение на домене магазина через обратный прокси и добавьте скрипт перед закрывающим тегом body:")}</p><pre>{'<script src="/widget.js" defer></script>'}</pre><p>{t("При подключении с другого домена кнопка открывает консультанта в отдельной вкладке. Это сохраняет работу сессии без ослабления защиты cookies.")}</p><p>{t("Встраивание на настоящий ekt.kz требует доступа владельца сайта. Прямое управление его корзиной требует отдельного API; демонстрационная корзина не создаёт заказов.")}</p></div></main>}
